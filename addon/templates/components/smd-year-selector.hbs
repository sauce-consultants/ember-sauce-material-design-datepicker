
{{smd-calendar-toolbar
  toggleAction='toggleMonthSelector'
  navAction='changeYearRange'
  label=yearRange
}}

{{#each years as |year|}}
  {{smd-selector-button
    for=this
    label=year.year
    selected=year.isSelected
    action='setYear'
    params=year
  }}
{{/each}}
